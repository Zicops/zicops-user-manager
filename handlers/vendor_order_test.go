package handlers

import (
	"testing"
)

func TestGetAllOrders(t *testing.T) {
	//run query in db and get result, run from api and get result and compare
	//either on mutation or queries

	// var ctx context.Context
	// a := ""
	// b := ""
	// c := ""
	// d := 0
	// res, err := GetAllOrders(ctx, &a, &b, &c, &d)
}
